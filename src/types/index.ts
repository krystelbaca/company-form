export interface State {
  name: string;
  abbreviation: string;
}

export interface CompanyType {
  id: string;
  name: string;
}

export interface Country {
  name: string;
  phone_code: string;
  flag_url: string;
}